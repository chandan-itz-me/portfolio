import SpiderWebSkills from "@/components/professional/SpiderWebSkills/SpiderWebSkills";

import styles from "./SkillsSection.module.css";

export default function SkillsSection() {
    return (
        <section className={styles.section}>
            <SpiderWebSkills />
        </section>
    );
}
